import Hero from "@/components/hero";
import React from "react";

const Home = () => {
  return (
    <div id="home" className="flex flex-col items-center h-main w-full">
      <Hero />
    </div>
  );
};

export default Home;
